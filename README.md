# tg-exchange-rate-bot

 - `pip install python-telegram-bot requests cachetools`
 - edit `currency_bot.py` and replace `SOME_TOKEN` to your bot token (replied by FatherBot)
 - exec `python currency_bot.py` to run 
